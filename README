#scion:  Lisp on Python

Scion is a lisp that runs atop the Python virtual machine and actually
generates Python bytecode.  It may use normal python functions and may define
functions that can be used from within Python code.

**Note that this is currently in pre-alpha stage**

##Installing

To install, simply run `python setup.py install`.